export const cubeWidth = () => window.innerWidth / cubeSizeFactor;
export const cubeHeight = () => window.innerHeight / cubeSizeFactor;
export const cubeDepth = () => cubeWidth();
export const cubeSizeFactor = 100;
export const zoomOutDistance = 2.5;

export const baseColor = 0x252525;
