import { html, TemplateResult } from "lit-html";

export const OnlineBookingInfo = () => {
  return html` <div>testing testing testing</div> `;
};

export const InfoModal = (component: TemplateResult) => {
  return html` <div>${component}</div> `;
};
